# MHY_Scanner 修复说明

## 改了什么（对应 3 个问题）

### ③ 退出后无法重开、要结束进程 —— 已彻底修复
- **文件**：`src/UI/WindowMain.cpp`
- **原因**：`closeEvent` 只 `t1.stop(); t2.stop();`，没有 `event->accept()`，也没有退出事件循环。
  进程残留占住了 `main.cpp` 里的命名互斥锁 `T08lJ8CJmJiyoxdV`，导致再次启动时 `isOpen()` 判定"已在运行"，
  然后 `FindWindowW` 找不到旧窗口 → `return 0` 静默退出。
- **改法**：closeEvent 里补 `t1.wait()/t2.wait()`、`event->accept()`、`QApplication::quit()`，保证进程真正退出、锁释放。
- **效果**：关窗后进程正常退出，可以立刻重新打开。

### ① 账号校验接口 `getUserFullInfo` 失效 —— 已改机制，还差一个盐值
- **文件**：`src/Core/ApiDefs.hpp`、`src/Core/MhyApi.hpp`、`src/UI/WindowLogin.cpp`
- **原因**：米游社 2.115.0 起该接口要求 **Cookie（stuid/stoken/mid）+ DS1 签名**，工具之前一个头都没带。
- **改法**：
  - 实现了 DS1 签名函数 `DataSignAlgorithmVersionGen1()`（原来是个空 stub）；
  - `getMysUserName` 增加 stoken/mid 参数，拼接 Cookie，带 DS 和 `x-rpc-*` 请求头；
  - `WindowLogin.cpp` 的 3 处调用点把 stoken/mid 传进去；
  - `GetRequestHeader` 的 app_version/sdk_version/UA 更新到 2.115.0 / 2.42.0（和盐版本对应）。
- **⚠️ 唯一遗留**：`ApiDefs.hpp` 里的 `mihoyobbs_salt_k2` 是占位符 `TODO_REPLACE_WITH_CURRENT_K2_SALT`。
  **必须填入 2.115.0 的 K2 盐后，DS 才会通过服务器校验。**
  获取方式：反编译米游社 APK 搜 `salt=`，或查 UIGF 盐表 / 同类开源工具的 `mihoyobbs_salt` 常量。
  填好这一个常量，①就彻底通。

### ② 抖音直播间无反应 —— 已改可配置
- **文件**：`src/Core/LiveStreamLink.cpp`
- **原因**：硬编码了一串会过期的抓包 Cookie，过期后抖音接口返回非 0 → 界面无反应。
- **改法**：程序启动后**优先读取 exe 同目录的 `douyin_cookie.txt`**，读到了就用文件里的新 Cookie，
  没有才退回内置旧值。Cookie 过期/换号时，只需更新这个 txt 文件，无需重新编译。
  - 用法：把从浏览器/Reqable 抓到的新 `Cookie` 一整行存进 `douyin_cookie.txt`（UTF-8 无 BOM）。

## 怎么应用

两种方式任选：
1. **git apply**：在仓库根目录执行 `git apply MHY_Scanner_fixes.patch`
2. **手动替换**：用 `MHY_Scanner_fixes/src/` 下对应路径的 5 个文件覆盖仓库同名文件。

## 改后必做
1. **填入 K2 盐**（`src/Core/ApiDefs.hpp` 的 `mihoyobbs_salt_k2`），否则①不生效。
2. 抖音若仍无反应，写 `douyin_cookie.txt` 放新 Cookie。
3. 重新编译验证。

## 未改动（实测正常，勿动）
- 扫码接口 `qrcode/scan`：新旧 host、两种 body 都通（无需改）。
- 二维码格式 / 游戏类型解析：`str[79:82]` 和末尾 24 位 ticket 均未变。
